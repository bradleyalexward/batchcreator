% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_batch.R
\name{create_batch}
\alias{create_batch}
\title{Create a randomised wet-lab batch of samples and generate a PDF pick list}
\usage{
create_batch(
  samples,
  id_col = "Unique_ID",
  strata_cols,
  omic,
  batch_size,
  seed = NULL,
  eligible_sample = "Yes",
  not_eligible_sample = "No",
  balance_strategy = c("proportional", "stratified"),
  sample_display_cols = NULL,
  biobank = NULL,
  biobank_id_col = "Unique_ID",
  biobank_omic_col = "Application",
  tube_id_col = "Tube_Specific_ID",
  used_col = NULL,
  used_yes = "Yes",
  used_no = "No",
  replicates_col = NULL,
  tubes_per_sample = 1,
  position_cols = c("Freezer", "Stage", "Rack", "Drawer", "Slot", "Box", "Position"),
  pdf_file = NULL,
  write_updates = FALSE,
  update_in_place = FALSE,
  samples_out = NULL,
  biobank_out = NULL,
  quiet = TRUE
)
}
\arguments{
\item{samples}{A \code{data.frame}/\code{tibble} or a file path to \code{.csv}, \code{.tsv}, or \code{.xlsx}.}

\item{id_col}{Name of the unique sample identifier column in \code{samples}.
Defaults to \code{"Unique_ID"}. Values must be unique and non-missing.}

\item{strata_cols}{Character vector of columns used to balance randomisation.
Balancing is performed on weightings defined by these columns.}

\item{omic}{Name of the omic status column in \code{samples} (e.g. "Proteomic").
Used to filter eligible samples (default "Yes") and optionally update processed samples to no longer be eligible in future batches (default "No"). These strings can be edited via eligible_sample or not_eligible_sample arguments.}

\item{batch_size}{Number of samples to select.}

\item{seed}{Optional integer seed for reproducibility. If \code{NULL}, a seed is generated and returned.}

\item{eligible_sample}{Character string to define eligibility for inclusion in batch. Default "Yes".}

\item{not_eligible_sample}{Character string to define non-eligibility for inclusion in batch. Default "No".}

\item{balance_strategy}{Either \code{"proportional"} (default) or \code{"stratified"}.
\code{"stratified"} Allocates per-level targets for each balancing variable based on eligible cohort proportions, then selects a random set of samples that best matches these targets across all variables simultaneously (prioritising under-represented groups when trade-offs are required).
\code{"proportional"} batches match the eligible cohort composition on average.}

\item{sample_display_cols}{Optional columns to print in the PDF for the selected batch.
If \code{NULL}, the report prints \code{id_col} plus \code{strata_cols}.}

\item{biobank}{Optional \code{data.frame} or file path. If \code{NULL}, tube lookup is skipped
and the report omits the biobank table.}

\item{biobank_id_col}{Column in \code{biobank} containing the same unique IDs as \code{samples[[id_col]]}.
Defaults to \code{"Unique_ID"}.}

\item{biobank_omic_col}{Column in \code{biobank} indicating the omic the sample relates to (default "Application").}

\item{tube_id_col}{Column in \code{biobank} giving the unique tube ID (default "Tube_Specific_ID").}

\item{used_col}{Optional column in \code{biobank} indicating if tube has been used (default \code{NULL}).
When supplied, only rows where \code{used_col == used_no} are considered available and selected tubes are updated to \code{used_yes}.}

\item{used_yes}{Value in \code{used_col} column indicating if tube has been used (default "Yes").
Only used when \code{used_col} is supplied.}

\item{used_no}{Value in \code{used_col} column indicating available tubes (default "No").
Only used when \code{used_col} is supplied.}

\item{replicates_col}{Optional column indicating replicate number (default \code{NULL}).
When supplied, the lowest replicate(s) are chosen first.}

\item{tubes_per_sample}{Integer number of tubes to pick per sample (default 1).
When \code{biobank} is provided, samples with fewer than this number of available tubes are reported in \code{insufficient_tubes} and are not marked as processed.}

\item{position_cols}{Character vector of biobank location columns to include in the tube pick list (i.e. row, column, box, etc).}

\item{pdf_file}{Output path for the PDF. If \code{NULL}, a timestamped file is created in the working directory.
Relative paths are resolved from the caller's working directory.}

\item{write_updates}{Logical; if \code{TRUE}, writes updated samples/biobank tables to disk (default \code{FALSE}).
omic col in samples file will be updated, and Batch_sample_processed will be updated (or created if not present) for successfully picked tubes. If \code{used_col} is supplied, it will also be updated in the biobank file. Files are only written after the PDF report renders successfully.}

\item{update_in_place}{Logical; if \code{TRUE}, overwrite the original input files (only when inputs are file paths).
Default \code{FALSE}, which writes new \verb{_UPDATED} files instead.}

\item{samples_out}{Optional output path for updated samples table. If \code{NULL} and \code{write_updates=TRUE},
an \verb{_UPDATED} path is generated (unless \code{update_in_place=TRUE}).}

\item{biobank_out}{Optional output path for updated biobank table. Same rules as \code{samples_out}.}

\item{quiet}{Logical; reduce rmarkdown output (default TRUE).}
}
\value{
A list with elements:
\describe{
\item{batch}{Selected rows from the eligible subset of \code{samples}.}
\item{updated_samples}{samples table with omic status updated for processed samples.}
\item{biobank_picklist}{Tube pick list for samples with enough available tubes (or \code{NULL} if \code{biobank} not provided).}
\item{updated_biobank}{Biobank table with selected tubes annotated for the batch and, when \code{used_col} is supplied, updated to \code{used_yes} (or \code{NULL}).}
\item{unmatched_samples}{Selected samples that could not be matched to any available tube (or \code{NULL}).}
\item{insufficient_tubes}{Selected samples with fewer than \code{tubes_per_sample} available tubes (or \code{NULL}).}
\item{seed}{The seed used.}
\item{pdf}{Path to the rendered PDF (or \code{NA} if PDF rendering dependencies are missing).}
\item{written_files}{Named character vector of written files (empty if \code{write_updates=FALSE}).}
}
}
\description{
Selects a balanced random batch of eligible samples for a chosen omic assay
(e.g. Proteomic), optionally matches to a biobank inventory to generate a tube
pick list, updates processing status in the sample table, and renders a PDF report.
}
\details{
This function is designed to aid with wet-lab batching:
\itemize{
\item Eligibility is controlled by a per-omic status column (e.g. Proteomic == "Yes")
\item Randomisation can be balanced across one or more variables of interest
\item Biobank lookup can skip used tubes and pick the next available replicate(s)
}
}
\examples{
samples_path <- system.file("extdata", "samples.csv", package = "batchcreator")
biobank_path <- system.file("extdata", "biobank.csv", package = "batchcreator")

\dontrun{
out <- create_batch(
  samples = samples_path,
  strata_cols = c("Sample_group", "Sex"),
  omic = "Proteomic",
  batch_size = 10,
  biobank = biobank_path,
  used_col = "Used",
  replicates_col = "Replicates",
  pdf_file = tempfile(fileext = ".pdf")
)
}

}
